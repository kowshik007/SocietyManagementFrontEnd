export interface ZipCode{
    records:[
            {
                officename: string,
                zipcode: number,
                districtname: string,
                statename: string
            }
        ]
}